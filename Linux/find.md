### **Common options:**

- . → current directory
    
- -name → exact name match
    
- -iname → case-insensitive name match
    
- -type f → only files
    
- -type d → only directories
    
- -size → by file size
    
- -exec → run a command on each result (advanced)